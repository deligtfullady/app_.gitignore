
  // IntelliJ API Decompiler stub source generated from a class file
  // Implementation of methods is not available

package android.support.constraint.solver;

final class Pools {
    private static final boolean DEBUG = false;

    private Pools() { /* compiled code */ }

    static class SimplePool <T> implements android.support.constraint.solver.Pools.Pool<T> {
        private final java.lang.Object[] mPool;
        private int mPoolSize;

        SimplePool(int maxPoolSize) { /* compiled code */ }

        public T acquire() { /* compiled code */ }

        public boolean release(T instance) { /* compiled code */ }

        public void releaseAll(T[] variables, int count) { /* compiled code */ }

        private boolean isInPool(T instance) { /* compiled code */ }
    }

    static interface Pool <T> {
        T acquire();

        boolean release(T t);

        void releaseAll(T[] ts, int i);
    }
}